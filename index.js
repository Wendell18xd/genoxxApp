/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import GenoxxApp from './src/GenoxxApp';

AppRegistry.registerComponent(appName, () => GenoxxApp);
